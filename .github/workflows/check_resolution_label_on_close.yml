on:
  issues:
    types: [closed]
jobs:
  check-resolution-label:
    runs-on: ubuntu-latest
    if: ${{ !startsWith(github.event.issue.labels.*.name, 'Resolution') }}
    steps:
      - run: echo Resolution label is not set


